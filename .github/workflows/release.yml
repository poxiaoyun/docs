name: Release

on:
  push:
    tags:
      - 'v*.*.*'
    branches:
      - 'main'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Manual tag override'
        required: false

env:
  GIT_VERSION: ${{ github.ref_name }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      # action 一律取 node24 运行时的大版本：Node 20 运行时已被 GitHub 弃用，
      # checkout@v4 / setup-node@v4 / docker/*@v3 会在每次运行产生
      # "Node.js 20 is deprecated" 告警。升级时按住这一条选版本。
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: make build

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Docker Setup Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Registries
        run: make login
        env:
          REGISTRY_USERNAME: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          REGISTRY_PASSWORD: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Release Docker Image
        run: make release-image

      - name: Set up Helm
        if: startsWith(github.ref, 'refs/tags/v')
        uses: azure/setup-helm@v4

      - name: Release Helm Chart
        if: startsWith(github.ref, 'refs/tags/v')
        run: make release-helm
        env:
          REGISTRY_USERNAME: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          REGISTRY_PASSWORD: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
