---
title: vDCU 动态切分模式
updated: '2025-12-14'
author: Rune Docs Team
description: 通过 DCU-Device-Plugin + vDCU-Scheduler（或 HAMi）实现 vDCU 动态切分与自动回收。
tags:
  - ecosystem
  - hygon
  - dcu
  - kubernetes
  - vdcu
---

## 概述

Kubernetes 集群通过设备插件 DCU-Device-Plugin 与 vDCU-Scheduler（或 HAMi 调度器）实现节点上 vDCU 的动态切分和自动回收。

k8s 任务通过资源字段 `hygon.com/dcunum`、`hygon.com/dcucores`、`hygon.com/dcumem` 进行 vDCU 动态切分请求，无需在使用 vDCU 前手动划分 vDCU 实例。

## 安装部署

建议给节点打上相应标签后使用 HAMi 模式的 DCU-Device-Plugin 管理 vDCU。

```bash
# 固定标签：部署设备插件时需要查询此标签
kubectl label nodes <node-name> dcu=on

kubectl apply -f vdcu-admission-webhook.yaml
kubectl apply -f vdcu-scheduler.yaml
kubectl apply -f k8s-dcu-plugin-hami.yaml
```

## 部署验证

查看相关 Pod：

```bash
kubectl get pod -n kube-system -owide | grep vdcu
```

查看节点资源注册：

```bash
kubectl describe node <node-name>
```

示例：Capacity 字段出现 `hygon.com/dcunum: 4` 表示节点有 4 个可用 vDCU 资源。

## 应用实例

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: vdcu-pytorch-demo
spec:
  containers:
    - name: vdcu-pytorch-demo
      image: image.sourcefind.cn:5000/dcu/admin/base/pytorch:2.1.0-ubuntu22.04-dtk24.04.2-py3.10
      command: ["/bin/bash", "-c", "--"]
      args: ["sleep infinity & wait"]
      resources:
        limits:
          hygon.com/dcunum: 1      # 申请 1 个 vDCU
          hygon.com/dcucores: 60   # 指定 vDCU 算力为 60%
          hygon.com/dcumem: 3000   # 指定 vDCU 显存为 3000MB
```

使用限制：每个 container 最多可以申请 1 张 vDCU。申请多于 1 张时，k8s 资源池会削减申请数量，仅 1 张 vDCU 生效。

## 卸载/重部署注意

若卸载 vDCU 动态切分组件或重新部署，除卸载上述三个组件外，还需手动清理遗留资源：

```bash
kubectl delete secrets -n kube-system admission-webhook-dcu-certs
kubectl delete mutatingwebhookconfigurations mutating-webhook-dcu-cfg
```
