---
title: 快速开始
updated: '2025-12-04'
author: Rune Docs Team
description: 从零开始，快速熟悉魔哈仓库核心功能。
tags:
  - moha
  - quickstart
  - guide
---

## 快速开始

---

本文适用于刚刚接触魔哈Moha的用户，目标是5分钟内熟悉魔哈仓库的模型下载和模型推理。希望帮助您设置开发环境，并顺利从魔哈仓库开始模型学习，使用和开发。

## 环境安装

在本地开发环境使用魔哈社区的模型， 我们推荐使用 Moha SDK下载模型。您可以使用如下命令安装 Moha SDK：

```bash
pip install moha
```

同时我们推荐您安装 Git 和 Git LFS ，这是模型管理包括上传所必须的工具。

## 模型下载

如果您在高带宽的机器上运行，推荐使用Moha命令行工具下载模型。该方法支持断点续传和模型高速下载，例如可以通过如下命令，将Qwen2.5-0.5B-Instruct模型，下载到当前路径下的"model-dir"目录。

```
moha download --model="Qwen/Qwen2.5-0.5B-Instruct" --local_dir ./model-dir
```

您也可以使用Moha Python SDK下载模型，该方法支持断点续传和模型高速下载：

```
from moha import snapshot_download
model_dir = snapshot_download("Qwen/Qwen2.5-0.5B-Instruct")
```

由于模型都是通过Git存储，所以也可以在安装Git LFS后，通过git clone的方式在本地下载模型，例如：

```
git lfs install
git clone https://moha.xiaoshiai.cn/Qwen/Qwen2.5-0.5B-Instruct.git
```

关于模型下载的详细说明，可参考模型下载文档。

同时，如果模型和 Moha SDK绑定，则只需要几行代码即可加载模型，同时Moha还支持通过AutoModel等接口来加载模型。如下是使用AutoModel和pipeline方式加载模型的示例：

### 使用AutoModel加载模型：

Moha支持原生的pipeline推理，同时也兼容了由Transformers，Diffusers等提供的AutoModel和Pipeline的加载。

例如在安装 Moha 和 transformers 之后，就可以通过如下代码进行LLM的推理。

```bash
pip install transformers
```

```python
from moha import AutoModelForCausalLM, AutoTokenizer

model_name = "Qwen/Qwen2.5-0.5B-Instruct"

model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype="auto",
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained(model_name)
```

### 使用Moha pipeline加载模型：

```python
from moha.pipelines import pipeline
word_segmentation = pipeline('word-segmentation',model='damo/nlp_structbert_word-segmentation_chinese-base')
```

## 模型推理

推理不同模态多种任务，pipeline是最简单、最快捷的方法。您可以使用开箱即用的pipeline执行跨不同模式的多种任务，下面是一个pipeline完整的运行示例：

```python
from moha.pipelines import pipeline
from moha.utils.constant import Tasks

inference_pipeline = pipeline(
    task=Tasks.auto_speech_recognition,
    model='iic/speech_paraformer-large-vad-punc_asr_nat-zh-cn-16k-common-vocab8404-pytorch',
    model_revision="v2.0.4")

rec_result = inference_pipeline('https://isv-data.oss-cn-hangzhou.aliyuncs.com/ics/MaaS/ASR/test_audio/asr_vad_punc_example.wav')
print(rec_result)
```

Moha兼容了Transformers提供的简单而统一的方法来加载预训练实例和tokenizer。这意味着您可以使用Moha加载AutoModel和AutoTokenizer等类。下面是一个大语言模型的完整的运行示例：

```python
from moha import AutoModelForCausalLM, AutoTokenizer

model_name = "Qwen/Qwen2.5-0.5B-Instruct"

model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype="auto",
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained(model_name)

prompt = "Give me a short introduction to large language model."
messages = [
    {"role": "system", "content": "You are Qwen, created by Alibaba Cloud. You are a helpful assistant."},
    {"role": "user", "content": prompt}
]
text = tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=True
)
model_inputs = tokenizer([text], return_tensors="pt").to(model.device)

generated_ids = model.generate(
    **model_inputs,
    max_new_tokens=512
)
generated_ids = [
    output_ids[len(input_ids):] for input_ids, output_ids in zip(model_inputs.input_ids, generated_ids)
]

response = tokenizer.batch_decode(generated_ids, skip_special_tokens=True)[0]
```

对于暂时未与Moha SDK做原生集成的模型，可以先从Moha上下载模型，然后通过其他的主流库实现模型推理，以SDXL-Turbo模型为例，完整的模型推理运行示例如下：

```python
from diffusers import AutoPipelineForText2Image
import torch
from moha import snapshot_download

model_dir = snapshot_download("AI-Moha/sdxl-turbo")

pipe = AutoPipelineForText2Image.from_pretrained(model_dir, torch_dtype=torch.float16, variant="fp16")
pipe.to("cuda")

prompt = "A cinematic shot of a baby racoon wearing an intricate italian priest robe."

image = pipe(prompt=prompt, num_inference_steps=1, guidance_scale=0.0).images[0]
image.save("image.png")
```

## 模型部署

您可以使用魔哈社区 SwingDeploy 来一键部署指定的模型到云资源上。SwingDeploy除了支持各种任务小模型的部署外，对于部分大语言模型，SwingDeploy支持部署后直接提供OpenAI API兼容的调用接口。

如果您希望在自有的GPU环境进行模型部署，我们推荐使用vLLM部署大语言模型，首先您需要设置环境变量来指定使用Moha上的模型：

```bash
export VLLM_USE_MOHA=True
```

vllm>=0.6支持 vLLM 内置的工具调用功能，部署命令：

```bash
vllm serve Qwen/Qwen2.5-7B-Instruct --enable-auto-tool-choice --tool-call-parser hermes
之后你可以像使用GPT’s tool calling那样来使用它。
```
魔哈Moha也将支持更多模型的部署，敬请期待。