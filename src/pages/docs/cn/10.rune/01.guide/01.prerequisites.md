---
title: 环境准备
updated: '2026-09-14'
description: 创建实例前先花两分钟自查：账号、角色、区域、工作空间、配额是否都就绪。
tags:
  - rune
  - prerequisites
---

# 环境准备

在 Rune 里创建任何东西之前，只需要确认三件事：**你用什么账号登录、你在租户里是什么角色、管理员帮你准备好了哪些资源**。
Rune 不需要你在电脑上安装软件，也没有命令行工具，所有操作都在浏览器页面里完成。

:::tip 为什么先做这一步
就像去健身房要先办卡、存包、换衣服，缺任何一步都会被挡在门口。
Rune 里最常见的「按钮点不动」，原因基本都是区域或工作空间没准备好。
:::

![工作空间列表：每行是名称、命名空间、状态与创建时间，页面左上角就是「区域」和「工作空间」两个选择器](/assets/screenshots/rune/workspace-01.png)

自查时先看这里的 **状态**：只有 `Ready` 的工作空间才能用来创建实例。如果列表是空的，页面会提示 **暂无可用的工作空间**，租户管理员可以在这页直接点 **去创建工作空间**。

## 自查清单

| 要确认的事 | 具体怎么看 |
| --- | --- |
| 我能登录 | 能正常进入控制台，没有提示账号或密码错误 |
| 我有租户 | 点右上角头像打开菜单，能看到「租户 xxx」，并且是你要用的那个 |
| 我选好了区域 | 页面左上角的**区域**选择器里已经选中一个可用区域（机房） |
| 我选好了工作空间 | 页面左上角的**工作空间**选择器里已经选中一间办公室 |
| 配额还够 | 创建时若提示资源不足，就是配额不够，需要管理员调整 |
| 模板和规格可用 | 应用市场里能看到模板，创建时能选到可用的资源规格 |

## 我的角色是什么

Rune 的租户角色有三种：**管理员**、**开发者**、**成员**（界面上就叫「成员」）。

| 角色 | 能用哪些功能 |
| --- | --- |
| 管理员 | 全部功能，还能管理成员、配额和工作空间 |
| 开发者 | 全部业务功能（创建实例、看指标和日志）；但租户的**成员**页签只有管理员能进 |
| 成员 | 只能看到「首页」和「应用市场」，看不到「工作台」和「可观测性」 |

:::info 怎么判断自己是不是管理员/开发者
看左侧菜单里有没有**工作台**和**可观测性**这两组。
有，说明你是管理员或开发者；没有，说明你是成员——这不是故障，是权限设置。
:::

![资源规格列表：左侧「资源管理」分组下选中「资源规格」，页头右上角可以切换集群](/assets/screenshots/rune/flavor-01.png)

**规格** 决定了实例能配多少 CPU、内存和加速卡。这些规格由平台管理员按集群预先定义，你自己创建不了；创建实例时下拉里选不到想要的配置，就是这一层没有配全。

## 需要管理员帮你准备好什么

下面这些资源你自己创建不了，需要租户管理员或平台管理员提前配置好：

| 资源 | 谁准备 | 没有会怎样 |
| --- | --- | --- |
| 租户与成员身份 | 平台管理员 | 登录后看不到任何业务内容 |
| 区域（集群）授权 | 平台管理员 | 顶部没有可选区域，页面提示**暂无可用的集群** |
| 工作空间 | 租户管理员 | 页面提示**暂无可用的工作空间**，无法创建实例 |
| 配额 | 租户管理员 | 创建时报资源不足 |
| 可用规格 | 平台管理员 | 创建时选不到能用的算力规格 |

:::warning 看到空页面不一定是出错
如果没有可用区域，页面会提示「暂无可用的集群，请联系管理员创建集群或为您分配集群」，这一页没有自助按钮，只能联系管理员。
如果当前区域下没有工作空间，会提示「暂无可用的工作空间，请联系管理员创建工作空间或为您分配工作空间」，租户管理员在这页能直接点**去创建工作空间**。
:::

## 结果确认

当左侧菜单能完整看到**首页 / 工作台 / 可观测性**三组（首页分组下有首页和应用市场两项），并且左上角**区域**和**工作空间**都选好了具体名称时，环境就算准备好了。

## 相关

- [工作空间](/rune/console/workspace)
- [配额](/rune/console/quota)
- [规格](/rune/console/flavor)
- [创建工作负载](/rune/guide/workloads)
