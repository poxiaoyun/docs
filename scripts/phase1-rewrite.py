#!/usr/bin/env python3
"""Script to rewrite Phase 1 high-impact files."""
import os

BASE = '/Volumes/datas/project/rune/runeDoc/docs/src/pages/docs/cn'

# === architecture.md ===
arch = """---
title: '平台概念'
updated: '2026-03-23'
---

本文介绍晓石 AI 平台的核心概念和整体结构，帮助您快速理解平台的组织方式和功能划分。

---

## 平台整体结构

晓石 AI 平台是面向企业的 AI 全生命周期管理平台，提供推理部署、模型微调、开发环境、模型仓库、LLM 网关等完整能力。平台由三大子产品和两大控制面组成。

```mermaid
graph TB
    subgraph "用户角色"
        Developer["👨‍💻 开发者"]
        TenantAdmin["👔 租户管理员"]
        PlatformAdmin["🛡️ 平台管理员"]
        APIClient["🤖 API 客户端"]
    end

    subgraph "两大控制面"
        Console["Console 租户门户<br/>面向开发者和租户管理员"]
        Boss["BOSS 管理门户<br/>面向平台管理员"]
    end

    Developer --> Console
    TenantAdmin --> Console
    PlatformAdmin --> Boss
    APIClient -->|"OpenAI 兼容 API"| Boss
```

---

## 三大子产品

### Rune — AI 工作台

面向 ML 工程师和数据科学家，提供 AI 工作负载管理能力：

| 功能模块 | 说明 | 典型场景 |
|---------|------|---------|
| **推理服务** | 部署模型为在线 API，自动分配 GPU 资源 | 部署 LLaMA、ChatGLM 等大语言模型 |
| **微调服务** | 基于预训练模型提交 SFT/LoRA 微调任务 | 使用私有数据进行领域微调 |
| **开发环境** | 启动 JupyterLab / VS Code Server 远程开发环境 | 交互式调试模型、数据探索 |
| **应用管理** | 部署自定义 AI 应用（Web 演示、API 服务等） | 部署 Gradio/Streamlit 应用 |
| **实验管理** | 跟踪 ML 实验的参数、指标，对比不同实验 | 记录超参搜索结果，选出最优配置 |
| **评测管理** | 对模型进行系统化的基准评测 | 使用标准评测集评估模型性能 |
| **存储卷** | 持久化存储，可挂载到任意实例，内置文件管理器 | 存放模型权重、训练数据 |
| **应用市场** | 预置模板库，一键部署主流框架 | 快速部署 vLLM、TGI、LLaMA-Factory |

:::tip
所有工作负载类型共享统一的部署流程：**选择模板 → 配置参数 → 选择规格 → 提交部署**。
:::

### Moha — 模型中心

平台内置的 AI 资产仓库，提供企业级模型/数据集/镜像管理：

| 功能模块 | 说明 | 典型场景 |
|---------|------|---------|
| **模型管理** | Git 式版本控制，支持文件浏览、分支管理、PR | 管理企业私有模型版本迭代 |
| **数据集管理** | 数据集上传、版本管理、格式预览 | 管理训练和评测数据集 |
| **镜像仓库** | 容器镜像管理，支持安全扫描 | 管理自定义训练/推理容器镜像 |
| **Space** | 在线展示空间，托管 Gradio/Streamlit 应用 | 打造模型体验 Demo |
| **组织管理** | 以组织为单位管理仓库和成员 | 按团队划分模型资产所有权 |
| **镜像同步** | 从 HuggingFace/ModelScope 等自动同步 | 将公开模型同步到企业内网 |

### ChatApp — 对话体验

即开即用的大模型对话窗口，无需编写代码：

| 功能模块 | 说明 | 典型场景 |
|---------|------|---------|
| **AI 对话** | 选择模型与 API Key，进行流式对话 | 体验私有部署的大模型效果 |
| **对话调试** | 实时调整 Temperature/TopP/MaxTokens 等参数 | 开发者调优 Prompt 和参数 |
| **多模型对比** | 左右双栏并排对话，对比输出质量 | 评估不同模型的回答质量 |
| **Token 管理** | 管理 API 访问令牌，设置用量限制 | 控制 API 调用频率和范围 |

---

## 两大控制面

### Console — 租户门户

面向**租户管理员、开发者和普通成员**的日常工作界面。

| 模块 | 核心功能 | 目标用户 |
|------|---------|---------|
| **Rune** | 推理、微调、开发环境、应用、实验、评测、存储卷 | 开发者 |
| **Moha** | 模型、数据集、镜像、Space、组织管理 | 开发者/数据工程师 |
| **ChatApp** | AI 对话体验、参数调试、多模型对比 | 所有用户 |
| **个人中心** | 个人资料、安全设置、SSH Key、API Key、租户切换 | 所有用户 |

### BOSS — 管理门户

面向**平台管理员**的全局管理界面。

| 模块 | 核心功能 | 目标用户 |
|------|---------|---------|
| **账户中心** | 用户管理、租户管理、成员角色分配 | 平台管理员 |
| **Rune 管理** | 集群纳管、资源池、GPU/NPU 规格、模板/产品、应用市场 | 平台管理员 |
| **LLM 网关** | 渠道配置、API Key、内容审查、审计记录、服务注册 | 平台管理员 |
| **数据仓库** | 平台级模型/数据集/镜像/Space 管理、镜像同步 | 平台管理员 |
| **平台设置** | 系统成员、全局配置、动态仪表盘 | 平台管理员 |

:::warning
Console 和 BOSS 是两个独立的管理界面，用户无法直接在两者之间跳转。普通用户使用 Console，只有平台管理员可以访问 BOSS。
:::

---

## 资源层级模型

平台采用五级资源隔离模型：**平台 → 租户 → 集群 → 工作空间 → 实例**。

```mermaid
graph TD
    Platform["🏢 平台<br/>全局管理"]
    
    Platform --> T1["🏛️ 租户 A<br/>组织/团队"]
    Platform --> T2["🏛️ 租户 B<br/>组织/团队"]
    
    T1 --> C1["☸️ 集群 1<br/>GPU 集群（华东）"]
    T1 --> C2["☸️ 集群 2<br/>NPU 集群（华北）"]
    
    C1 --> W1["📁 工作空间 Dev"]
    C1 --> W2["📁 工作空间 Prod"]
    C2 --> W3["📁 工作空间 Train"]
    
    W1 --> I1["🚀 推理实例"]
    W1 --> I2["🔧 开发环境"]
    W2 --> I3["📊 评测实例"]
    W3 --> I4["⚡ 微调实例"]

    style Platform fill:#e1f5fe,stroke:#0288d1
    style T1 fill:#f3e5f5,stroke:#7b1fa2
    style T2 fill:#f3e5f5,stroke:#7b1fa2
```

### 各层级说明

| 层级 | 管理者 | 说明 |
|------|--------|------|
| **平台** | 系统管理员 | 全局管理所有租户、集群和配置。通过 BOSS 管理门户操作 |
| **租户** | 租户管理员 | 业务隔离的顶层单元，对应一个组织或团队。不同租户之间数据完全隔离 |
| **集群** | 系统管理员 | 实际承载计算资源的基础设施，支持 GPU、NPU 等异构算力 |
| **工作空间** | 租户管理员 | 最小的资源隔离单元，开发者日常操作的核心容器 |
| **实例** | 开发者 | 各类 AI 工作负载的统称（推理、微调、开发环境、应用、实验、评测） |

---

## 资源配置流程

从集群纳管到开发者部署实例，涉及不同角色的协作配置：

```mermaid
flowchart TD
    subgraph "阶段一：基础设施准备（系统管理员 · BOSS）"
        A1["导入集群"] --> A2["创建资源池<br/>划分 GPU/NPU 分区"]
        A2 --> A3["配置规格<br/>定义 GPU 型号/数量/CPU/内存"]
        A3 --> A4["发布模板<br/>配置应用市场"]
    end

    subgraph "阶段二：配额分配（系统管理员 · BOSS）"
        A4 --> B1["分配集群配额<br/>为租户设置资源上限"]
        B1 --> B2["分配租户可用规格"]
    end

    subgraph "阶段三：工作空间管理（租户管理员 · Console）"
        B2 --> C1["创建工作空间"]
        C1 --> C2["配置工作空间配额<br/>从租户配额中划分"]
        C2 --> C3["添加成员并分配角色"]
    end

    subgraph "阶段四：实例部署（开发者 · Console）"
        C3 --> D1["选择实例类型"]
        D1 --> D2["选择规格"]
        D2 --> D3["选择模板 + 配置参数"]
        D3 --> D4["挂载存储卷"]
        D4 --> D5["提交部署"]
    end

    style A1 fill:#e3f2fd
    style B1 fill:#f3e5f5
    style C1 fill:#e8f5e9
    style D1 fill:#fff3e0
```

### 规格（Flavor）三级继承

规格采用**集群 → 租户 → 工作空间**三级继承模型。系统管理员在集群上定义所有可用规格，然后分配给租户，租户管理员再将其中一部分分配给工作空间。

```mermaid
graph TD
    CF["集群级规格<br/>所有 GPU/CPU/NPU 规格"] --> TF["租户级规格<br/>分配给该租户的子集"]
    TF --> WF["工作空间级规格<br/>分配给该工作空间的子集"]
    WF --> Instance["开发者创建实例时选择"]
```

:::warning
资源配额也遵循相同的三级继承。工作空间配额不能超过租户配额，租户配额不能超过集群可用资源总量。
:::

---

## LLM 网关

LLM 网关是大语言模型的统一调用代理层，提供企业级的 API 管理能力。

```mermaid
graph LR
    Client["API 客户端"]
    GW["LLM 网关"]
    LLM1["OpenAI"]
    LLM2["本地推理服务"]
    LLM3["其他供应商"]

    Client -->|"OpenAI 兼容 API"| GW
    GW --> LLM1
    GW --> LLM2
    GW --> LLM3
```

**核心能力**：

| 能力 | 说明 |
|------|------|
| **统一接入** | 将多个 LLM 供应商封装为统一的 OpenAI 兼容 API |
| **渠道路由** | 根据模型名称自动匹配渠道，支持优先级和降级 |
| **API Key 管理** | 为不同用户/团队创建独立的 API Key，设置用量配额 |
| **内容审查** | 对请求和响应内容进行安全检查（关键词匹配、AI 审核等） |
| **审计日志** | 记录每次 API 调用的完整信息（Token 用量、耗时、渠道等） |
| **限速保护** | 按 API Key 或全局设置请求频率和 Token 用量限制 |

:::tip
平台内部署的推理服务可以通过「服务注册」接入 LLM 网关，统一通过网关 API 调用，无需直接暴露推理服务地址。
:::

---

## 存储体系

平台提供基于对象存储的持久化存储能力。

```mermaid
graph TD
    subgraph "数据来源"
        Upload["Web 页面上传"]
        Git["Git 仓库"]
        HF["HuggingFace"]
        MS["ModelScope"]
        Moha["Moha 仓库"]
    end

    subgraph "平台存储"
        Vol["存储卷"]
    end

    subgraph "使用方"
        Inference["推理服务"]
        Finetune["微调任务"]
        DevEnv["开发环境"]
    end

    Upload --> Vol
    Git --> Vol
    HF --> Vol
    MS --> Vol
    Moha --> Vol

    Vol --> Inference
    Vol --> Finetune
    Vol --> DevEnv
```

**存储卷使用流程**：
1. **创建**存储卷，指定名称和容量
2. **导入数据**：通过页面上传，或创建存储任务从 Git/HuggingFace/ModelScope 等来源同步
3. **挂载使用**：在部署实例时关联存储卷，实例启动后即可访问
4. **删除**：需先停止所有关联实例，再删除存储卷

:::warning
存储卷一旦挂载到运行中的实例，无法直接删除。需先停止或删除关联实例后才能操作。
:::

---

## 监控与日志

平台为实例、集群和网关提供全方位的运行监控。

### 实例级监控

| 能力 | 说明 |
|------|------|
| **运行指标** | GPU 利用率、显存使用、CPU、内存、网络 IO |
| **容器日志** | 标准输出/错误日志，支持关键词搜索和实时流 |
| **终端访问** | 直接进入容器的交互式命令行终端 |
| **监控面板** | 动态生成的可视化监控仪表盘 |

### 集群级监控

| 能力 | 说明 |
|------|------|
| **集群仪表盘** | 节点总览、资源使用率、调度情况 |
| **日志查询** | 集群范围的日志检索 |
| **资源监控** | GPU/NPU 池使用率、配额消耗趋势 |

### 网关运营数据

| 能力 | 说明 |
|------|------|
| **使用记录** | 每次 API 调用的 Token 用量、耗时、模型、渠道 |
| **审计记录** | 完整的请求/响应审计轨迹 |
| **运营面板** | 请求数趋势、Token 用量统计、渠道分布、错误率 |

---

## 下一步

- [术语表](./glossary) — 了解平台核心术语定义
- [角色与权限](../auth/roles) — 了解权限体系
- [快速上手](./quick-start) — 从零开始的操作指南
- [Console 概览](../console/) — 开始使用 Console 门户
"""

with open(os.path.join(BASE, '02.guide', '02.architecture.md'), 'w', encoding='utf-8') as f:
    f.write(arch.lstrip('\n'))

print(f"architecture.md: {len(arch.strip().splitlines())} lines")

# === api-overview.md ===
api = """---
title: 'API 集成概述'
updated: '2026-03-23'
description: '如何通过 API 接口与晓石 AI 平台集成'
tags:
  - reference
  - api
---

本文介绍如何通过 API 与晓石 AI 平台进行集成，主要面向需要通过程序调用平台能力的开发者。

---

## 平台服务架构

晓石 AI 平台提供 5 个核心服务域，覆盖认证、计算、存储、模型仓库和 LLM 网关：

```mermaid
graph TB
    subgraph "客户端"
        App["您的应用程序"]
    end

    subgraph "平台服务"
        IAM["IAM 服务<br/>认证 · 用户 · 租户 · 角色"]
        Cloud["Cloud 服务<br/>集群 · 工作空间 · 实例"]
        AI["AI 服务<br/>存储卷 · 文件管理"]
        Moha["Moha 服务<br/>模型 · 数据集 · 镜像"]
        AIRouter["LLM 网关<br/>OpenAI 兼容 API"]
    end

    App --> IAM
    App --> Cloud
    App --> AI
    App --> Moha
    App -->|"最常用"| AIRouter
```

---

## 认证方式

所有 API 请求需在请求头中携带认证信息：

| Header | 说明 | 示例值 |
|--------|------|--------|
| `Authorization` | 认证令牌 | `Bearer eyJhbGci...`（平台 Token）或 `Bearer sk-xxx`（LLM 网关 Key） |
| `Content-Type` | 请求格式 | `application/json` |

:::tip
LLM 网关使用独立的 API Key（`sk-` 前缀），可在 Console → 个人中心 → API Key 管理 中创建。平台其他服务使用登录后获取的 Token。
:::

---

## LLM 网关 API（最常用）

LLM 网关提供与 **OpenAI API 完全兼容**的接口，是最常用的集成方式。您可以直接使用 OpenAI 官方 SDK：

### Python 示例

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxxxxxxxxxxxxxxx",
    base_url="https://your-platform.com/api/airouter/v1"
)

response = client.chat.completions.create(
    model="deepseek-v3",
    messages=[
        {"role": "system", "content": "你是一个有帮助的助手。"},
        {"role": "user", "content": "什么是 Kubernetes？"}
    ],
    stream=True
)

for chunk in response:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="")
```

### cURL 示例

```bash
curl -X POST https://your-platform.com/api/airouter/v1/chat/completions \\
  -H "Authorization: Bearer sk-xxxxxxxxxxxxxxxx" \\
  -H "Content-Type: application/json" \\
  -d '{
    "model": "deepseek-v3",
    "messages": [{"role": "user", "content": "你好"}],
    "stream": false
  }'
```

:::tip
将 `base_url` 指向平台地址即可使用任何兼容 OpenAI SDK 的客户端库（Python、Node.js、Java 等）。
:::

---

## 常见 API 响应状态码

| 状态码 | 含义 | 说明 |
|--------|------|------|
| `200` | 请求成功 | 正常返回数据 |
| `201` | 创建成功 | 资源已创建 |
| `204` | 删除成功 | 无响应体 |
| `400` | 请求参数错误 | 检查请求参数 |
| `401` | 未认证 | Token 无效或已过期 |
| `403` | 无权限 | 当前用户无权执行此操作 |
| `404` | 资源不存在 | 检查资源 ID 或路径 |
| `409` | 资源冲突 | 名称已存在等 |
| `429` | 请求频率超限 | 降低调用频率后重试 |
| `500` | 服务器错误 | 联系平台管理员 |

---

## 速率限制

| 接口类型 | 限制 | 说明 |
|---------|------|------|
| LLM 网关 | 按 API Key 配置 | 管理员可设置每分钟请求数（RPM）和每分钟 Token 数（TPM） |
| 登录接口 | 5 次/分钟 | 防止暴力破解 |
| 文件上传 | 单文件最大 10GB | 超大文件建议使用对象存储 CLI 工具 |

:::warning
触发速率限制时平台返回 `429` 状态码，请降低调用频率后重试。具体限制数值由平台管理员配置。
:::

---

## 下一步

- [权限说明](./permissions) — 了解角色和权限体系
- [常见问题](./faq) — 常见问题解答
"""

with open(os.path.join(BASE, '50.reference', '01.api-overview.md'), 'w', encoding='utf-8') as f:
    f.write(api.lstrip('\n'))

print(f"api-overview.md: {len(api.strip().splitlines())} lines")

# === permissions.md ===
perms = """---
title: '角色与权限'
updated: '2026-03-23'
description: '晓石 AI 平台的角色定义、权限说明和各角色功能对比'
tags:
  - reference
  - permissions
---

本文介绍晓石 AI 平台的角色体系和权限模型，帮助您了解不同角色的操作范围和功能边界。

---

## 权限模型总览

平台采用**三级作用域**的权限模型：系统级 → 租户级 → 工作空间级。上级作用域的角色拥有更大的操作范围。

```mermaid
graph TD
    System["🌐 系统级<br/>平台全局管理"]
    Tenant1["🏢 租户 A"]
    Tenant2["🏢 租户 B"]
    WS1["📁 工作空间 A-1"]
    WS2["📁 工作空间 A-2"]
    WS3["📁 工作空间 B-1"]

    System --> Tenant1
    System --> Tenant2
    Tenant1 --> WS1
    Tenant1 --> WS2
    Tenant2 --> WS3
```

| 作用域 | 说明 | 管理界面 | 示例 |
|--------|------|---------|------|
| **系统级** | 平台全局管理，可管理所有租户和资源 | BOSS | 系统管理员管理整个平台 |
| **租户级** | 限定在某个租户范围内 | Console | 租户管理员管理本租户的工作空间和成员 |
| **工作空间级** | 限定在某个工作空间内 | Console | 开发者在工作空间中创建和管理实例 |

---

## 角色定义

平台预定义了 4 个角色：

| 角色 | 作用域 | 可访问界面 | 说明 |
|------|--------|----------|------|
| **系统管理员** | 系统级 | BOSS + Console | 平台最高权限，可管理所有租户、集群、用户 |
| **租户管理员** | 租户级 | Console | 管理本租户下所有工作空间、成员和资源 |
| **开发者** | 租户级 | Console | 可创建和管理实例，只读查看工作空间和模板等 |
| **成员** | 租户级 | Console | 仅有查看权限 |

:::tip
一个用户在不同租户中可以拥有不同角色。例如，张三在租户 A 是管理员，在租户 B 是开发者。切换租户时，可用的功能会随角色变化。
:::

---

## 角色权限对比

### 资源操作权限

| 资源 | 操作 | 系统管理员 | 租户管理员 | 开发者 | 成员 |
|------|------|:---:|:---:|:---:|:---:|
| **工作空间** | 查看 | ✅ | ✅ | ✅ | ✅ |
| **工作空间** | 创建/编辑/删除 | ✅ | ✅ | ❌ | ❌ |
| **实例** | 查看 | ✅ | ✅ | ✅ | ✅ |
| **实例** | 创建/启停/删除 | ✅ | ✅ | ✅ | ❌ |
| **实例** | 查看日志/终端 | ✅ | ✅ | ✅ | ❌ |
| **镜像** | 查看 | ✅ | ✅ | ✅ | ✅ |
| **镜像** | 创建/编辑/删除 | ✅ | ✅ | ❌ | ❌ |
| **模板** | 查看 | ✅ | ✅ | ✅ | ❌ |
| **模板** | 创建/编辑/删除 | ✅ | ✅ | ❌ | ❌ |
| **成员** | 管理 | ✅ | ✅ | ❌ | ❌ |
| **配额** | 管理 | ✅ | ✅ | ❌ | ❌ |
| **存储卷** | 管理 | ✅ | ✅ | ❌ | ❌ |

### 功能入口权限

| 功能 | 系统管理员 | 租户管理员 | 开发者 | 成员 |
|------|:---:|:---:|:---:|:---:|
| BOSS 管理后台 | ✅ | ❌ | ❌ | ❌ |
| Console 控制台 | ✅ | ✅ | ✅ | ✅ |
| 部署推理服务 | ✅ | ✅ | ✅ | ❌ |
| 提交微调任务 | ✅ | ✅ | ✅ | ❌ |
| 创建开发环境 | ✅ | ✅ | ✅ | ❌ |
| 实验追踪 | ✅ | ✅ | ✅ | ❌ |
| 工作空间管理 | ✅ | ✅ | ❌ | ❌ |
| 成员管理 | ✅ | ✅ | ❌ | ❌ |
| 配额管理 | ✅ | ✅ | ❌ | ❌ |
| 集群管理 | ✅ | ❌ | ❌ | ❌ |
| 用户管理 | ✅ | ❌ | ❌ | ❌ |

---

## 权限生效规则

- **最小权限原则**：用户默认无权限，只有被明确授予角色后才获得对应权限
- **系统管理员特权**：系统管理员自动拥有所有租户和工作空间的全部权限
- **自动刷新**：登录、切换租户或工作空间时，平台自动更新权限。无权限的菜单和操作按钮会自动隐藏

:::warning
页面上隐藏或禁用的操作按钮表示您当前角色无权执行该操作。如需更多权限，请联系您的租户管理员或系统管理员。
:::

---

## 常见场景

### 场景一：新成员加入

1. 租户管理员在 Console → 成员管理 中添加新用户
2. 为其分配角色（开发者 / 成员）
3. 新用户登录后选择对应租户即可开始工作

### 场景二：开发者需要创建工作空间

开发者角色没有创建工作空间的权限，需要：
1. 联系租户管理员创建工作空间
2. 租户管理员为工作空间分配配额和规格
3. 租户管理员将开发者添加为工作空间成员

### 场景三：查看操作被拒绝

如果访问某个页面显示 403（无权限），可能原因：
- 当前登录的租户不正确 — 尝试切换租户
- 角色权限不足 — 联系管理员提升角色
- 工作空间未分配 — 联系管理员添加为工作空间成员

---

## 下一步

- [API 集成概述](./api-overview) — 了解平台 API 接口
- [常见问题](./faq) — 常见问题解答
"""

with open(os.path.join(BASE, '50.reference', '02.permissions.md'), 'w', encoding='utf-8') as f:
    f.write(perms.lstrip('\n'))

print(f"permissions.md: {len(perms.strip().splitlines())} lines")
print("Done!")
