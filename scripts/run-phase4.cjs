const path = require('path'); const fs = require('fs'); const { migrateFile } = require('./migrate.cjs'); function run() { const cn = path.resolve(__dirname, '../src/pages/docs/cn/10.rune'); const en = path.resolve(__dirname, '../src/pages/docs/en/10.rune'); [cn, en].forEach(dir => { ['03.console', '04.boss', '05.chatapp'].forEach(sub => fs.mkdirSync(path.join(dir, sub), { recursive: true })); }); [ ['../../docs-ai/console/rune', path.join(cn, '03.console')], ['../../docs-ai/en/console/rune', path.join(en, '03.console')], ['../../docs-ai/boss/rune', path.join(cn, '04.boss')], ['../../docs-ai/en/boss/rune', path.join(en, '04.boss')], ['../../docs-ai/console/chatapp', path.join(cn, '05.chatapp')], ['../../docs-ai/en/console/chatapp', path.join(en, '05.chatapp')] ].forEach(([src, dest]) => { const s = path.resolve(__dirname, src); if (fs.existsSync(s)) { fs.readdirSync(s).filter(f => f.endsWith('.md') && f !== 'index.md').forEach(f => migrateFile(path.join(s, f), path.join(dest, f))); } }); } run();
